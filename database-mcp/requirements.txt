# ── Core ──
fastmcp>=3.0.0               # FastMCP framework for MCP protocol

# ── Production database drivers ──
oracledb>=2.0.0          # Oracle (thin mode — no Oracle Client needed)
impyla>=0.18.0           # Impala (HiveServer2 protocol)
google-cloud-bigquery>=3.0.0  # BigQuery
pg8000>=1.30.0           # CloudSQL PostgreSQL
PyMySQL>=1.1.0           # CloudSQL MySQL
